import { DefaultPageLayout } from "../../shared/layouts"

export const Dashboard = () => {

  return (
    <DefaultPageLayout title="Home Page" toolBar={<>Toolbar</>}>
      Testando
    </DefaultPageLayout>
  );
}